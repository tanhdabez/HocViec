@model Core.Request.DanhMucLoaiHangResponse

@{
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Chi tiết Loại Hàng</h4>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4"><i class="bi bi-person"></i> Tên</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Ten)</dd>

                        <dt class="col-sm-4"><i class="bi bi-file-text"></i>Ghi Chú</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.GhiChu)</dd>

                        <dt class="col-sm-4"><i class="bi bi-check-circle"></i> Trạng thái</dt>
                        <dd class="col-sm-8">
                            @if (Model.TrangThai)
                            {
                                <span class="text-success"><strong>Hoạt Động</strong></span>
                            }
                            else
                            {
                                <span class="text-danger"><strong>Dừng hoạt động</strong></span>
                            }
                        </dd>
                        <dt class="col-sm-4"><i class="bi bi-calendar-check"></i> Ngày cập nhật gần nhất</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.UpdateDate)</dd>
                    </dl>
                </div>
                <div class="card-footer text-center">
                    <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-primary">
                        Cập nhật
                    </a>
                    <a asp-action="Index" class="btn btn-secondary">
                        Quay lại danh sách
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>